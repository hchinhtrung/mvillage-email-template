# Modern free crawl stack — no paid proxies, no paid APIs.
curl_cffi>=0.11          # TLS/JA3/HTTP2 impersonation for direct replay (Firefox + Chrome targets)
camoufox[geoip]>=0.4.11  # anti-detect Firefox for the once-per-run warm (run: `camoufox fetch`)
# Playwright pin is LOAD-BEARING for Camoufox:
#   * 1.60.* crashed Camoufox Firefox (daijro/camoufox#617)
#   * 1.61+ sends viewport.isMobile that Camoufox Juggler rejects
#     (daijro/camoufox#653: "Found property ... isMobile ... not described in this scheme")
# Stay on 1.59.x until Camoufox ships a Juggler that accepts isMobile.
playwright>=1.59,<1.60
playwright-stealth>=1.0.6
pandas>=1.5
openpyxl>=3.0            # .xlsx hotel lists
nest-asyncio>=1.5        # notebook driver (re-entrant event loop)

# After install also run:
#   python -m playwright install chromium
#   python -m camoufox fetch
# Then verify the interpreter you'll crawl with:
#   python -m crawler doctor

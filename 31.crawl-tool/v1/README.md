# Trip crawler — v1 (freeze)

Snapshot của code đang chạy ổn (trước khi thêm speed-up).

## Chạy

Mở [`run_trip.ipynb`](run_trip.ipynb) và chạy các cell từ trên xuống.

- Package: `v1/crawler/` (không sửa)
- Input/results: dùng chung `../input/`, `../results/trip/`

## Khi nào dùng v1

- `v2` bị block nhiều / kết quả sai
- Cần baseline ổn định để so sánh

Root [`../run_trip.ipynb`](../run_trip.ipynb) + [`../crawler/`](../crawler/) cũng là bản gốc tương đương v1 (không bị đụng).
